#include <stdio.h> 
#include <string.h>

const char* determine_color(const char* color) { 
    // Write your logic here to determine the color based on the sting s.
    // Return "Black" or "White" based on the problem statement.
     return NULL; 
     }

     int main(){ 
      char s[256];
      scanf("%s" , s);
       const char* result = determine_color(s);
       printf("%s\n", result);
     return 0;
    }
